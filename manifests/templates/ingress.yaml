---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: react-vite-landing-page-keanghor-d840b523                  # Ingress name
  labels:
    app: react-vite-landing-page-keanghor-d840b523
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "react-vite-landing-page-keanghor-d840b523.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: react-vite-landing-page-keanghor-d840b523               # Links to Service name dynamically
                port:
                  number: 5173  # Links to service port dynamically
